"use strict";

import {connect} from "react-redux";
import Application from "../presentational/sc";

let mapStateToProps = (state) => {

} 

let mapDispatchToProps = (dispatch) => {

}

let visibleApp = connect(
    mapStateToProps,
    mapDispatchToProps
)(Application);

export default visibleApp;